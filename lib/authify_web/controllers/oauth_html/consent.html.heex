<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Authorize Application - Authify</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
    />
  </head>
  <body class="bg-body-secondary">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
          <div class="card mt-5">
            <div class="card-header bg-primary text-white text-center">
              <h4 class="mb-0">
                <i class="bi bi-shield-check"></i> Authorize Application
              </h4>
            </div>
            <div class="card-body">
              <div class="text-center mb-4">
                <div class="d-flex align-items-center justify-content-center mb-3">
                  <div class="text-center">
                    <i class="bi bi-person-circle display-4 text-muted"></i>
                    <div class="small text-muted">You</div>
                  </div>
                  <div class="mx-3">
                    <i class="bi bi-arrow-right text-muted"></i>
                  </div>
                  <div class="text-center">
                    <i class="bi bi-app-indicator display-4 text-primary"></i>
                    <div class="small text-muted">Application</div>
                  </div>
                </div>

                <h5><strong>{@application.name}</strong></h5>
                <p class="text-muted mb-0">wants to access your Authify account</p>
              </div>

              <%= if @application.description do %>
                <div class="alert alert-light">
                  <small>{@application.description}</small>
                </div>
              <% end %>

              <div class="mb-4">
                <h6>This application will be able to:</h6>
                <ul class="list-unstyled">
                  <%= for scope <- @scopes do %>
                    <li class="mb-2">
                      <i class="bi bi-check-circle text-success me-2"></i>
                      {scope_description(scope)}
                    </li>
                  <% end %>
                </ul>
              </div>

              <div class="row">
                <div class="col-6">
                  <form method="post" action="/oauth/consent">
                    <input
                      type="hidden"
                      name="_csrf_token"
                      value={Plug.CSRFProtection.get_csrf_token()}
                    />
                    <input type="hidden" name="client_id" value={@application.client_id} />
                    <input type="hidden" name="redirect_uri" value={@redirect_uri} />
                    <input type="hidden" name="scope" value={Enum.join(@scopes, " ")} />
                    <%= if @state do %>
                      <input type="hidden" name="state" value={@state} />
                    <% end %>
                    <%= if @code_challenge do %>
                      <input type="hidden" name="code_challenge" value={@code_challenge} />
                    <% end %>
                    <%= if @code_challenge_method do %>
                      <input
                        type="hidden"
                        name="code_challenge_method"
                        value={@code_challenge_method}
                      />
                    <% end %>
                    <input type="hidden" name="approve" value="false" />
                    <button type="submit" class="btn btn-outline-secondary w-100">
                      <i class="bi bi-x-circle"></i> Deny
                    </button>
                  </form>
                </div>
                <div class="col-6">
                  <form method="post" action="/oauth/consent">
                    <input
                      type="hidden"
                      name="_csrf_token"
                      value={Plug.CSRFProtection.get_csrf_token()}
                    />
                    <input type="hidden" name="client_id" value={@application.client_id} />
                    <input type="hidden" name="redirect_uri" value={@redirect_uri} />
                    <input type="hidden" name="scope" value={Enum.join(@scopes, " ")} />
                    <%= if @state do %>
                      <input type="hidden" name="state" value={@state} />
                    <% end %>
                    <%= if @code_challenge do %>
                      <input type="hidden" name="code_challenge" value={@code_challenge} />
                    <% end %>
                    <%= if @code_challenge_method do %>
                      <input
                        type="hidden"
                        name="code_challenge_method"
                        value={@code_challenge_method}
                      />
                    <% end %>
                    <input type="hidden" name="approve" value="true" />
                    <button type="submit" class="btn btn-primary w-100">
                      <i class="bi bi-check-circle"></i> Authorize
                    </button>
                  </form>
                </div>
              </div>

              <div class="text-center mt-3">
                <small class="text-muted">
                  You can revoke access at any time from your account settings
                </small>
              </div>
            </div>
          </div>

          <div class="text-center mt-3">
            <small class="text-muted">
              Powered by <strong>Authify</strong> - Secure Identity Provider
            </small>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
